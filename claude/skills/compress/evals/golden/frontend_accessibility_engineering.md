# Frontend Accessibility Engineering

Now nav update -> focus loss because modal remounts to refresh stale props and handlers.

Preserve `aria-label` values and use `useMemo` for stable keyboard flow.

Patch `/src/components/NavBar.tsx`, keep Ctrl+K and Ctrl+N docs aligned.

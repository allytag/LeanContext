# Security Incident Response

On-call must preserve exact log identifiers for incident timeline.

Record `AUTH_TOKEN_EXPIRED`, `RateLimitError`, `2026-04-17T22:40:00Z` exactly as printed.

Do not rewrite stack traces, payload text, or API names.
